name: luma_web
description: Luma collaborative workspace — web client.

publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  go_router: ^17.1.0
  http: ^1.2.0
  web: ^1.1.0
  qr_flutter: ^4.1.0
  pdf: ^3.10.8
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
