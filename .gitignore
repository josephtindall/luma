# ── Secrets & Environment ─────────────────────────────────────────────────────
.env
.env.local
.env.*.local
*.env

# Allow the example file — safe to commit
!.env.example

# ── Go ────────────────────────────────────────────────────────────────────────
# Compiled binaries
/luma
/bin/
/dist/

# Test output
*.test
*.out
coverage.html
coverage.out

# Go workspace (local only)
go.work
go.work.sum

# Vendor directory
vendor/

# ── Flutter — Web ─────────────────────────────────────────────────────────────
luma-web/.dart_tool/
luma-web/.flutter-plugins
luma-web/.flutter-plugins-dependencies
luma-web/.packages
luma-web/build/
luma-web/pubspec.lock

# Generated files
luma-web/**/*.g.dart
luma-web/**/*.freezed.dart
luma-web/**/*.mocks.dart

# ── Flutter — Mobile ──────────────────────────────────────────────────────────
luma-mobile/.dart_tool/
luma-mobile/.flutter-plugins
luma-mobile/.flutter-plugins-dependencies
luma-mobile/.packages
luma-mobile/build/
luma-mobile/pubspec.lock

# Generated files
luma-mobile/**/*.g.dart
luma-mobile/**/*.freezed.dart
luma-mobile/**/*.mocks.dart

# Android
luma-mobile/android/.gradle/
luma-mobile/android/captures/
luma-mobile/android/gradlew
luma-mobile/android/gradlew.bat
luma-mobile/android/local.properties
luma-mobile/android/key.properties
luma-mobile/android/app/google-services.json
luma-mobile/android/**/*.keystore
luma-mobile/android/**/*.jks

# iOS
luma-mobile/ios/.symlinks/
luma-mobile/ios/Flutter/Flutter.framework
luma-mobile/ios/Flutter/Flutter.podspec
luma-mobile/ios/Flutter/Generated.xcconfig
luma-mobile/ios/Flutter/app.flx
luma-mobile/ios/Flutter/app.zip
luma-mobile/ios/Flutter/flutter_assets/
luma-mobile/ios/Flutter/flutter_export_environment.sh
luma-mobile/ios/ServiceDefinitions.json
luma-mobile/ios/Runner/GeneratedPluginRegistrant.*
luma-mobile/ios/Pods/
luma-mobile/ios/Podfile.lock

# macOS (if Flutter desktop added later)
luma-mobile/macos/Flutter/GeneratedPluginRegistrant.swift
luma-mobile/macos/Flutter/Flutter-Debug.xcconfig
luma-mobile/macos/Flutter/Flutter-Release.xcconfig

# ── Certificates & Keys ───────────────────────────────────────────────────────
*.pem
*.key
*.crt
*.p12
*.pfx
*.jks
/certs/

# ── Docker ────────────────────────────────────────────────────────────────────
docker-compose.local.yml
docker-compose.override.yml

# ── Database ──────────────────────────────────────────────────────────────────
*.sql.bak
/pgdata/

# ── IDE & Editor ──────────────────────────────────────────────────────────────
# VS Code
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example

# JetBrains
.idea/
*.iml

# Vim / Neovim
*.swp
*.swo
*~

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
logs/

# ── Temp & Build ──────────────────────────────────────────────────────────────
tmp/
temp/
.tmp/
*.tmp

# ── SQLite (offline cache — generated at runtime, never committed) ─────────────
*.sqlite
*.sqlite3
*.db

# ── Test Fixtures ─────────────────────────────────────────────────────────────
testdata/generated/

# ── Claude Code ───────────────────────────────────────────────────────────────
.claude/sessions/
